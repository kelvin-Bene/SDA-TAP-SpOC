# Exclude frontend from backend deploys (separate service)
frontend/
# Exclude test/dev files
tests/
validation/
docs/
*.md
# Exclude top-level data files (mounted/generated at runtime)
# Use explicit path to avoid excluding uct_benchmark/data/ source code
/data/
# Exclude environment files
.env
.env.*
