// Stub — real implementation lands in P2-M5.
// Exists now so App.tsx can lazy-import it without breaking the build.

export function LeaderboardChatPage() {
  return (
    <div className="container mx-auto py-8 max-w-3xl">
      <h1 className="text-2xl font-display font-bold tracking-tight">Chat Assistant</h1>
      <p className="text-muted-foreground mt-2">
        Coming soon — multi-turn conversational interface with the catalog. (M5 of Phase 2.)
      </p>
    </div>
  );
}
